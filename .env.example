# Django Configuration
# Copy this file to .env and update with your actual values

# Django superuser credentials (used by create_superuser management command)
DJANGO_SUPERUSER_USERNAME=admin
DJANGO_SUPERUSER_EMAIL=admin@stagdb.local
DJANGO_SUPERUSER_PASSWORD=changeme123

# Django secret key (generate a new one for production)
# You can generate a new key using: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=django-insecure-replace-this-with-a-real-secret-key

# Debug mode (set to False in production)
DEBUG=True
